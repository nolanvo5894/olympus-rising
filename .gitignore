node_modules/
dist/
.vite/

# Python
__pycache__/
*.pyc
.venv/

# Environment
.env
.env.*
!.env.example

.DS_Store

# Scraping scripts (kept locally, not tracked)
scripts/scrape_*.py

# Raw scrape outputs (kept locally, not tracked).
# Inputs to build_unified_dataset.py only; the unified CSVs are tracked
# so the compute scripts can be re-run from a fresh clone.
data/olympic_results_global.csv
data/paralympic_medalists_global.csv
data/usa_athlete_profiles.csv
